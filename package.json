{
  "name": "urban-age-website",
  "version": "1.0.0",
  "description": "Urban Age website with form integration",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step needed for static site'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "resend": "^3.0.0",
    "googleapis": "^128.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
